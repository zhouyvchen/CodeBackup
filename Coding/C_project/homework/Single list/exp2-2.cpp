#include "linklist.cpp"
int main() {
    LinkNode* h;
    ElemType e;
    printf("单链表的基本运算如下:\n");
    printf("  (1)依次采用头插法插入a,b,c,d,e元素\n");
            CreateListF(h,a,5;
	printf("  (2)输出单链表h:"); 
            printf("  (2)输出单链表h:");
	DispList(h);
	printf("  (3)单链表h长度:%d\n",ListLength(h));
	printf("  (4)单链表h为%s\n",(ListEmpty(h)?"空":"非空"));
	GetElem(h,3,e);

    void CreateListF(LinkNode*&L, E